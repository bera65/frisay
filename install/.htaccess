# Kurulum tamamlandıktan sonra web erişimini kapatır.
# Yeni kurulum için geçici olarak bu dosyayı devre dışı bırakın.
<IfModule mod_authz_core.c>
	Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
	Order deny,allow
	Deny from all
</IfModule>
