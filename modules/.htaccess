# Modül PHP dosyalarına doğrudan HTTP erişimini engelle (assets/css, assets/js serbest)
<FilesMatch "\.php$">
	<IfModule mod_authz_core.c>
		Require all denied
	</IfModule>
	<IfModule !mod_authz_core.c>
		Order deny,allow
		Deny from all
	</IfModule>
</FilesMatch>
