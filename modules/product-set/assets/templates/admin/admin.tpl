{if $flash}
<div class="alert alert-{$flashType|default:'info'}">{$flash|escape}</div>
{/if}
<p class="mb-0">Ürün setlerini <strong>Ürünler → ürün düzenle</strong> ekranından yönetin. Ürün tipini <em>Set (paket)</em> seçip bileşenleri ekleyin.</p>
