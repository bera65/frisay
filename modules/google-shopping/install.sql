-- Google Shopping modülü için ayrı tablo gerekmez.
-- Ayarlar Settings (settings tablosu) üzerinden yönetilir.
-- Bu dosya install() metodunda runSqlFile ile çağrılmaz; install() true döner.
SELECT 1;
